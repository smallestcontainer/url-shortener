[project]
name = "shortener"
version = "0.0.0"
dependencies = [
    "fastapi[standard]",
    "hashids",
    "pydantic"
]

[project.optional-dependencies]
dev = [ "pytest" ]

[build-system]
requires = ["setuptools >= 84.0.0", "msgpack>=1.2.1"]
build-backend = "setuptools.build_meta"


[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]